{
  "name": "elm-pages-example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "elm-pages develop",
    "serve": "elm-pages develop",
    "debug": "elm-pages develop --debug",
    "build": "elm-pages build",
    "static": "npm run build && http-server ./dist -a localhost -p 3000 -c-1",
    "postinstall": "elm-tooling install",
    "review": "rm -rf gen/; elm-review --fix"
  },
  "dependencies": {
    "elm-pages": "^1.0.8",
    "undefined": "github:avh4/elm-color"
  },
  "devDependencies": {
    "elm-tooling": "^1.3.0",
    "http-server": "^0.12.3"
  }
}
